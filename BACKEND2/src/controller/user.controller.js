import asycHandaler from '../utils/asycHandaler.js';

const rejesterUser = asycHandaler(async (req, res) => {
  //if we wand ro rjester a user in our webside we have to follw thes stape
  //1. take data from frantent and filter them in different veribal like for avtar add in diff ver and usere detail in dictunary or array
  //2. second add them in data if data is currect and data is succesufully add in data base then redirect to home page and send them you have succefull rejester and if not then send message somting is wrong plase give write data and some other massahe.
});

export default rejesterUser;
